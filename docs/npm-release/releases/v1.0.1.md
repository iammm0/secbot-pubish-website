# v1.0.1

- Release date: 2026-03-15
- Git tag: `v1.0.1`

## Summary

### Features

- Expanded the Python agent layer with planner, QA, and specialist-oriented behavior.
- Improved the terminal UI message blocks, including `UserMessageBlock`.
- Added LLM fallback documentation and HTTP fallback logic.
- Added the `terminal_tool` path for terminal-side execution workflows.

### Timeline Notes

- Representative commit: `86fceb5` (`feat: 智能体与 TUI 增强、LLM 回退与文档...`).
- This release sits between the initial public Python package and the later TUI-centered rewrite work.
